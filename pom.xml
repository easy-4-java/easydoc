<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.github.easy4j</groupId>
    <artifactId>easydoc</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <url>https://github.com/easy-4-java/${project.artifactId}</url>
    <version>${revision}</version>
    <description>Building doc documents based on xhtml templates using docx4j</description>
    <packaging>pom</packaging>

    <!-- 开源协议采用 Apache 2.0 协议 -->
    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <!-- 源码仓库（SCM）信息 -->
    <scm>
        <connection>scm:git:https://github.com/easy-4-java/easydoc.git</connection>
        <developerConnection>scm:git:https://github.com/easy-4-java/easydoc.git</developerConnection>
        <url>https://github.com/easy-4-java/easydoc</url>
        <tag>${project.artifactId}</tag>
    </scm>

    <!-- 开发者信息 -->
    <developers>
        <developer>
            <name>Loong Wan</name>
            <email>hiwepy@gmail.com</email>
            <url>https://github.com/loong10k</url>
            <roles>
                <role>developer</role>
            </roles>
            <timezone>+8</timezone>
        </developer>
    </developers>



    <!-- 版本属性定义：Java 版本、依赖版本与 Maven 插件版本统一管理 -->
    <properties>
        <!-- ═══ CI-friendly 版本：单一来源，Maven 3 需 flatten-maven-plugin 将 ${revision} 展开 ═══ -->
        <revision>2.0.x.20260730-SNAPSHOT</revision>

        <!-- ═══ 核心配置：JDK / 编译 / 编码 ═══ -->
        <java.version>17</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.version>3.9.16</maven.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- ═══ Docx4j 全家桶版本（11.5.14 统一锁定） ═══ -->
        <docx4j.version>11.5.14</docx4j.version>
        <docx4j-export-fo.version>11.5.14</docx4j-export-fo.version>
        <docx4j-importxhtml.version>11.5.14</docx4j-importxhtml.version>
        <docx4j-jaxb.version>11.5.14</docx4j-jaxb.version>
        <docx4j-openxml.version>11.5.14</docx4j-openxml.version>

        <!-- ═══ Jakarta XML Binding（JAXB 4）与 Activation ═══ -->
        <jakarta-activation.version>2.1.3</jakarta-activation.version>

        <!-- ═══ 模板引擎版本 ═══ -->
        <beetl.version>3.1.3.RELEASE</beetl.version>
        <freemarker.version>2.3.34</freemarker.version>
        <httl.version>1.0.12</httl.version>
        <jetbrick.version>2.1.10</jetbrick.version>
        <rythm.version>1.3.0</rythm.version>
        <thymeleaf.version>3.0.14.RELEASE</thymeleaf.version>
        <velocity.version>2.4.1</velocity.version>
        <velocity-tools.version>2.0</velocity-tools.version>
        <webit-script.version>1.5.2</webit-script.version>

        <!-- ═══ Third-Party Dependencies（按字母序） ═══ -->
        <antlr4.version>4.7.1</antlr4.version>
        <assertj.version>3.27.3</assertj.version>
        <commons-beanutils.version>1.11.0</commons-beanutils.version>
        <commons-io.version>2.22.0</commons-io.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <commons-logging.version>1.3.5</commons-logging.version>
        <jackson-bom.version>2.22.1</jackson-bom.version>
        <javax.mail-api.version>1.6.2</javax.mail-api.version>
        <jsoup.version>1.15.1</jsoup.version>
        <junit.version>4.13.2</junit.version>
        <junit-jupiter.version>6.1.0</junit-jupiter.version>
        <ognl.version>3.2.13</ognl.version>
        <persistence.version>2.7.6</persistence.version>
        <servlet-api.version>3.0.1</servlet-api.version>
        <slf4j.version>2.0.18</slf4j.version>

        <!-- ═══ Maven Plugin versions ═══ -->
        <maven-antrun-plugin.version>1.8</maven-antrun-plugin.version>
        <maven-assembly-plugin.version>3.1.1</maven-assembly-plugin.version>
        <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-dependency-plugin.version>3.8.1</maven-dependency-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
        <maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <maven-install-plugin.version>3.1.4</maven-install-plugin.version>
        <maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-nexus-staging-plugin.version>1.7.0</maven-nexus-staging-plugin.version>
        <maven-nexus-yuicompressor-plugin.version>1.1.0</maven-nexus-yuicompressor-plugin.version>
        <maven-release-plugin.version>3.3.1</maven-release-plugin.version>
        <maven-resources-plugin.version>3.5.0</maven-resources-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-surefire-plugin.version>2.22.1</maven-surefire-plugin.version>
        <maven-war-plugin.version>3.2.2</maven-war-plugin.version>
        <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>

        <!-- ═══ 工具库版本 ═══ -->
        <jaxb-namespaceprefixmapper.version>6.1.0</jaxb-namespaceprefixmapper.version>
        <lombok.version>1.18.46</lombok.version>
    </properties>

    <!-- 制品发布仓库配置（distributionManagement） -->
    <distributionManagement>
        <repository>
            <id>2624322-release-6F6h6R</id>
            <url>https://packages.aliyun.com/6927b116e6c3e0425dbdf60d/maven/2624322-release-6f6h6r</url>
        </repository>
        <snapshotRepository>
            <id>2624322-snapshot-3EoOv3</id>
            <url>https://packages.aliyun.com/6927b116e6c3e0425dbdf60d/maven/2624322-snapshot-3eoov3</url>
        </snapshotRepository>
    </distributionManagement>

    <!-- 项目子模块（modules） -->
    <modules>
        <module>easydoc-beetl</module>
        <module>easydoc-bom</module>
        <module>easydoc-core</module>
        <module>easydoc-freemarker</module>
        <module>easydoc-httl</module>
        <module>easydoc-jetbrick</module>
        <module>easydoc-jsp</module>
        <module>easydoc-rythm</module>
        <module>easydoc-thymeleaf</module>
        <module>easydoc-velocity</module>
        <module>easydoc-webit</module>
        <module>easydoc-xhtml</module>
    </modules>

    <!-- 构建配置（Build） -->
    <build>
        <pluginManagement>
            <plugins>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-clean-plugin -->
                <plugin>
                    <!-- 清理插件：清理构建输出目录 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven-clean-plugin.version}</version>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
                <plugin>
                    <!-- 编译插件：编译主代码至主输出目录 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <!-- 字符集编码 -->
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <!-- 编译最大内存 -->
                        <maxmem>512M</maxmem>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-deploy-plugin -->
                <plugin>
                    <!-- 发布插件：将项目输出构件部署到远程仓库 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-enforcer-plugin -->
                <plugin>
                    <!-- 环境检查插件：代码编译前的环境监察 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>default-cli</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <phase>validate</phase>
                            <configuration>
                                <rules>
                                    <requireMavenVersion>
                                        <message>
	                                        You are running an older version of Maven. This application requires at least Maven ${maven.version}.
										</message>
                                        <version>[${maven.version}.0,)</version>
                                    </requireMavenVersion>
                                    <requireJavaVersion>
                                        <message>
	                                        You are running an older version of Java. This application requires at least JDK ${java.version}.
										</message>
                                        <version>[${java.version}.0,)</version>
                                    </requireJavaVersion>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-gpg-plugin -->
                <plugin>
                    <!-- 签名插件：对制品进行 PGP 签名 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven-gpg-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>sign-artifacts</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>sign</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-install-plugin -->
                <plugin>
                    <!-- 安装插件：将项目输出构件安装到本地仓库 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven-install-plugin.version}</version>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-resources-plugin -->
                <plugin>
                    <!-- 资源插件：复制主资源文件至主输出目录 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-release-plugin -->
                <plugin>
                    <!-- 发布管理插件：版本发布与标签管理 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>${maven-release-plugin.version}</version>
                    <configuration>
                        <tagNameFormat>v@{project.version}</tagNameFormat>
                        <autoVersionSubmodules>true</autoVersionSubmodules>
                        <useReleaseProfile>false</useReleaseProfile>
                        <releaseProfiles>release</releaseProfiles>
                        <goals>deploy</goals>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-source-plugin -->
                <plugin>
                    <!-- 源码插件：发布时自动将源码同时发布 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <configuration>
                        <attach>true</attach>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-surefire-plugin -->
                <plugin>
                    <!-- 单元测试插件：执行测试用例 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                    <configuration>
                        <skip>false</skip>
                        <skipTests>false</skipTests>
                        <forkMode>once</forkMode>
                        <argLine>-Xmx1024m -Dfile.encoding=UTF-8 @{argLine}</argLine>
                        <additionalClasspathElements>
                            <additionalClasspathElement>${basedir}/target/test-classes</additionalClasspathElement>
                        </additionalClasspathElements>
                        <includes>
                            <include>**/*Test.java</include>
                        </includes>
                        <excludes>
                            <exclude>**/TestBean.java</exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-jar-plugin -->
                <plugin>
                    <!-- jar 包生成插件：创建项目 jar 包 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                    <configuration>
                        <skipIfEmpty>true</skipIfEmpty>
                        <archive>
                            <manifest>
                                <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                                <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                            </manifest>
                        </archive>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-javadoc-plugin -->
                <plugin>
                    <!-- 文档插件：发布时自动生成 Javadoc 并发布 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <!-- 原生 doclint=none：additionalparam 在 javadoc-plugin 3.x 已失效 -->
                        <doclint>none</doclint>
                        <charset>${project.build.sourceEncoding}</charset>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <docencoding>${project.build.sourceEncoding}</docencoding>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <phase>package</phase>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.sonatype.plugins/nexus-staging-maven-plugin -->
                <plugin>
                    <groupId>org.sonatype.plugins</groupId>
                    <artifactId>nexus-staging-maven-plugin</artifactId>
                    <version>${maven-nexus-staging-plugin.version}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <serverId>ossrh</serverId>
                        <nexusUrl>https://oss.sonatype.org/</nexusUrl>
                        <autoReleaseAfterClose>false</autoReleaseAfterClose>
                        <stagingProgressPauseDurationSeconds>60</stagingProgressPauseDurationSeconds>
                        <stagingProgressTimeoutMinutes>20</stagingProgressTimeoutMinutes>
                        <detectBuildFailures>true</detectBuildFailures>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>

            <!-- https://mvnrepository.com/artifact/org.jacoco/jacoco-maven-plugin -->
            <plugin>
                <!-- 覆盖率插件：生成覆盖率报告并校验测试覆盖率（目标 90%） -->
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <haltOnFailure>false</haltOnFailure>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.90</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- flatten-maven-plugin：Maven 3 CI-friendly 支持，将 ${revision} 展开为实际版本 -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.8.0</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
            <plugin>
                <!-- 编译插件：编译主代码至主输出目录 -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <!-- 保留方法参数名，支持 Spring MVC 按参数名绑定 -->
                    <parameters>true</parameters>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <maxmem>512M</maxmem>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- ═══ 构建 Profile 配置 ═══ -->
    <profiles>
        <!-- 自动激活（JDK 1.8+）：禁用 Javadoc doclint、锁定 Lombok/JaCoCo 版本 -->
        <profile>
            <id>disable-javadoc-doclint</id>
            <activation>
                <jdk>[1.8,)</jdk>
            </activation>
            <properties>
                <central-publishing-maven-plugin.version>0.11.0</central-publishing-maven-plugin.version>
                <lombok.version>1.18.46</lombok.version>
                <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
            
		<jaxb-namespaceprefixmapper.version>6.1.0</jaxb-namespaceprefixmapper.version></properties>
        </profile>
        <!-- 发布到 Maven Central Portal：mvn -Prelease deploy（GPG 签名 + 源码 + Javadoc + Central 发布插件） -->

        <profile>
            <id>release</id>
            <build>
                <plugins>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-enforcer-plugin -->
                    <plugin>
                        <!-- 环境检查插件：代码编译前的环境监察 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-enforcer-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
                    <plugin>
                        <!-- 编译插件：编译主代码至主输出目录 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-resources-plugin -->
                    <plugin>
                        <!-- 资源插件：复制主资源文件至主输出目录 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-surefire-plugin -->
                    <plugin>
                        <!-- 单元测试插件：执行测试用例 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-jar-plugin -->
                    <plugin>
                        <!-- jar 包生成插件：创建项目 jar 包 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-source-plugin -->
                    <plugin>
                        <!-- 源码插件：发布时自动将源码同时发布 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-javadoc-plugin -->
                    <plugin>
                        <!-- 文档插件：发布时自动生成 Javadoc 并发布 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-install-plugin -->
                    <plugin>
                        <!-- 安装插件：将项目输出构件安装到本地仓库 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-gpg-plugin -->
                    <plugin>
                        <!-- 签名插件：对制品进行 PGP 签名 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-deploy-plugin -->
                    <plugin>
                        <!-- 发布插件：将项目输出构件部署到远程仓库 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-release-plugin -->
                    <plugin>
                        <!-- 发布管理插件：版本发布与标签管理 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-release-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.sonatype.plugins/nexus-staging-maven-plugin -->
                    <plugin>
                        <groupId>org.sonatype.plugins</groupId>
                        <artifactId>nexus-staging-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- 发布到 Maven Central Portal：mvn -Pcentral deploy（GPG 签名 + 源码 + Javadoc + Central 发布插件） -->

        <profile>
            <id>central</id>
            <build>
                <plugins>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-source-plugin -->
                    <plugin>
                        <!-- 源码插件：发布时自动将源码同时发布 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-sources</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-javadoc-plugin -->
                    <plugin>
                        <!-- 文档插件：发布时自动生成 Javadoc 并发布 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-gpg-plugin -->
                    <plugin>
                        <!-- 签名插件：对制品进行 PGP 签名 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven-gpg-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.sonatype.central/central-publishing-maven-plugin -->
                    <plugin>
                        <!-- 发布插件：将制品发布到 Maven Central Portal -->
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central-publishing-maven-plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <!-- 依赖版本统一管理（dependencyManagement） -->
    <dependencyManagement>
        <dependencies>
            <!-- ======== BOM ======== -->

            <!-- Jackson BOM：统一管理 Jackson 组件版本 -->
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ======== Test ======== -->

            <!-- https://mvnrepository.com/artifact/junit/junit -->
            <!-- JUnit 4：单元测试框架 -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter -->
            <!-- JUnit 5（聚合包）：新一代单元测试框架，包含 api + engine + params -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit-jupiter.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter-api -->
            <!-- JUnit 5 API：编写测试用例的公共 API -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit-jupiter.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.assertj/assertj-core -->
            <!-- AssertJ：流式断言库，提供更优雅的测试断言 API -->
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
            </dependency>

            <!-- ======== Servlet ======== -->

            <!-- https://mvnrepository.com/artifact/javax.servlet/javax.servlet-api -->
            <!-- Servlet API：JSP 模块编译期依赖，运行时由容器提供 -->
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>${servlet-api.version}</version>
            </dependency>

            <!-- ======== Logging ======== -->

            <!-- https://mvnrepository.com/artifact/org.slf4j/slf4j-api -->
            <!-- SLF4J API：日志门面，所有模块统一使用此版本 -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.slf4j/slf4j-simple -->
            <!-- SLF4J Simple：轻量级日志实现，用于测试环境 -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/commons-beanutils/commons-beanutils -->
            <dependency>
                <groupId>commons-beanutils</groupId>
                <artifactId>commons-beanutils</artifactId>
                <version>${commons-beanutils.version}</version>
            </dependency>
            <!-- ======== Commons ======== -->

            <!-- https://mvnrepository.com/artifact/org.apache.commons/commons-lang3 -->
            <!-- Commons Lang3：字符串、对象、数组等常用工具方法 -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/commons-io/commons-io -->
            <!-- Commons IO：文件/流操作工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>

            <!-- ======== HTML / CSS ======== -->

            <!-- https://mvnrepository.com/artifact/org.jsoup/jsoup -->
            <!-- Jsoup：HTML 解析与操作库，用于 XHTML 模板处理 -->
            <dependency>
                <groupId>org.jsoup</groupId>
                <artifactId>jsoup</artifactId>
                <version>${jsoup.version}</version>
            </dependency>
            <!-- ======== Docx4j 全家桶（11.5.14） ======== -->

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-core -->
            <!-- Docx4j Core：docx/pptx/xlsx 的 Open XML 解析与生成核心库 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-core</artifactId>
                <version>${docx4j.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.slf4j</groupId>
                        <artifactId>jcl-over-slf4j</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.slf4j</groupId>
                        <artifactId>slf4j-log4j12</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>log4j</groupId>
                        <artifactId>log4j</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-export-fo -->
            <!-- Docx4j Export FO：通过 Apache FOP 将 docx 导出为 PDF -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-export-fo</artifactId>
                <version>${docx4j-export-fo.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-docx-anon -->
            <!-- Docx4j Docx Anon：docx 文档匿名化（去除作者等元数据） -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-docx-anon</artifactId>
                <version>${docx4j.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-diffx -->
            <!-- Docx4j Diffx：XML 差异比较工具 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-diffx</artifactId>
                <version>${docx4j.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-ImportXHTML-core -->
            <!-- Docx4j ImportXHTML：将 XHTML/HTML 片段导入为 docx 内容（核心模块） -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-ImportXHTML-core</artifactId>
                <version>${docx4j-importxhtml.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-JAXB-ReferenceImpl -->
            <!-- Docx4j JAXB ReferenceImpl：docx4j 的 JAXB 实现，替代旧版 MOXy -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-JAXB-ReferenceImpl</artifactId>
                <version>${docx4j-jaxb.version}</version>
            </dependency>

            <!-- ======== Jakarta XML Binding 4（替代 javax.xml.bind） ======== -->

            <!-- https://mvnrepository.com/artifact/com.googlecode.jaxb-namespaceprefixmapper-interfaces/JAXBNamespacePrefixMapper -->
            <!-- JAXB NamespacePrefixMapper：自定义 XML 命名空间前缀 -->
            <dependency>
                <groupId>com.googlecode.jaxb-namespaceprefixmapper-interfaces</groupId>
                <artifactId>JAXBNamespacePrefixMapper</artifactId>
                <version>${jaxb-namespaceprefixmapper.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/jakarta.xml.bind/jakarta.xml.bind-api -->
            <!-- Jakarta JAXB 4 API：替代 javax.xml.bind:jaxb-api，docx4j 11.x 需要 -->
            <dependency>
                <groupId>jakarta.xml.bind</groupId>
                <artifactId>jakarta.xml.bind-api</artifactId>
                <version>4.0.2</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.glassfish.jaxb/jaxb-runtime -->
            <!-- JAXB Runtime（GlassFish）：Jakarta JAXB 4 的运行时实现 -->
            <dependency>
                <groupId>org.glassfish.jaxb</groupId>
                <artifactId>jaxb-runtime</artifactId>
                <version>4.0.6</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/jakarta.activation/jakarta.activation-api -->
            <!-- Jakarta Activation API：MIME 类型处理，JAXB 运行时依赖 -->
            <dependency>
                <groupId>jakarta.activation</groupId>
                <artifactId>jakarta.activation-api</artifactId>
                <version>${jakarta-activation.version}</version>
            </dependency>

            <!-- ======== EclipseLink ======== -->

            <!-- https://mvnrepository.com/artifact/org.eclipse.persistence/eclipselink -->
            <!-- EclipseLink：JPA 实现，docx4j MOXy JAXB 依赖（测试/兼容） -->
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>eclipselink</artifactId>
                <version>${persistence.version}</version>
            </dependency>

            <!-- ======== Docx4j Open XML Objects ======== -->

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-openxml-objects -->
            <!-- Docx4j OpenXML Objects：docx（Word）格式的 JAXB 对象模型 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-openxml-objects</artifactId>
                <version>${docx4j-openxml.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-openxml-objects-pml -->
            <!-- Docx4j OpenXML Objects PML：pptx（PowerPoint）格式的 JAXB 对象模型 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-openxml-objects-pml</artifactId>
                <version>${docx4j-openxml.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-openxml-objects-sml -->
            <!-- Docx4j OpenXML Objects SML：xlsx（Excel）格式的 JAXB 对象模型 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-openxml-objects-sml</artifactId>
                <version>${docx4j-openxml.version}</version>
            </dependency>
            <!-- ======== 模板引擎及其依赖 ======== -->

            <!-- https://mvnrepository.com/artifact/ognl/ognl -->
            <!-- OGNL：表达式语言引擎，beetl/httl 等模板引擎的底层依赖 -->
            <dependency>
                <groupId>ognl</groupId>
                <artifactId>ognl</artifactId>
                <version>${ognl.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/com.ibeetl/beetl -->
            <!-- Beetl：高性能 Java 模板引擎，语法简洁 -->
            <dependency>
                <groupId>com.ibeetl</groupId>
                <artifactId>beetl</artifactId>
                <version>${beetl.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.freemarker/freemarker -->
            <!-- FreeMarker：经典 Java 模板引擎，MVC 架构常用 -->
            <dependency>
                <groupId>org.freemarker</groupId>
                <artifactId>freemarker</artifactId>
                <version>${freemarker.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.perfcake/httl -->
            <!-- HTTL：高性能模板引擎，编译型，适合大流量场景 -->
            <dependency>
                <groupId>org.perfcake</groupId>
                <artifactId>httl</artifactId>
                <version>${httl.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/com.github.subchen/jetbrick-template -->
            <!-- Jetbrick Template：轻量级 Java 模板引擎 -->
            <dependency>
                <groupId>com.github.subchen</groupId>
                <artifactId>jetbrick-template</artifactId>
                <version>${jetbrick.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.antlr/antlr4-runtime -->
            <!-- ANTLR4 Runtime：解析器生成器运行时，Rythm 引擎依赖 -->
            <dependency>
                <groupId>org.antlr</groupId>
                <artifactId>antlr4-runtime</artifactId>
                <version>${antlr4.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.rythmengine/rythm-engine -->
            <!-- Rythm：类 Razor 语法的高性能 Java 模板引擎 -->
            <dependency>
                <groupId>org.rythmengine</groupId>
                <artifactId>rythm-engine</artifactId>
                <version>${rythm.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.thymeleaf/thymeleaf -->
            <!-- Thymeleaf：自然模板引擎，支持直接在浏览器预览 -->
            <dependency>
                <groupId>org.thymeleaf</groupId>
                <artifactId>thymeleaf</artifactId>
                <version>${thymeleaf.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.apache.velocity/velocity-engine-core -->
            <!-- Velocity Engine Core：Apache 经典模板引擎核心 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.apache.velocity/velocity-engine-scripting -->
            <!-- Velocity Engine Scripting：Velocity 的 JSR-223 脚本引擎适配 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-scripting</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- https://mvnrepository.com/artifact/org.apache.velocity/velocity-tools -->
            <!-- Velocity Tools：Velocity 的工具集（日期/数字/链接等） -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-tools</artifactId>
                <version>${velocity-tools.version}</version>
                <exclusions>
                    <exclusion>
                        <artifactId>struts-core</artifactId>
                        <groupId>org.apache.struts</groupId>
                    </exclusion>
                    <exclusion>
                        <artifactId>struts-taglib</artifactId>
                        <groupId>org.apache.struts</groupId>
                    </exclusion>
                    <exclusion>
                        <artifactId>struts-tiles</artifactId>
                        <groupId>org.apache.struts</groupId>
                    </exclusion>
                </exclusions>
            </dependency>
            <!-- ======== Webit ======== -->

            <!-- https://mvnrepository.com/artifact/com.github.zqq90.webit-script/webit-script -->
            <!-- Webit Script：轻量级 Java 模板引擎 -->
            <dependency>
                <groupId>com.github.zqq90.webit-script</groupId>
                <artifactId>webit-script</artifactId>
                <version>${webit-script.version}</version>
            </dependency>

            <!-- ======== 工具库 ======== -->

            <!-- https://mvnrepository.com/artifact/org.projectlombok/lombok -->
            <!-- Lombok：编译期注解处理器，自动生成 getter/setter/builder 等样板代码 -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 项目依赖（dependencies） -->
    <dependencies>
        <!-- For Slf4j -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <!-- For Junit -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
                <!-- For JUnit 5 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- For AssertJ -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- For Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
