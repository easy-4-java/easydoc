<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.github.easy4j</groupId>
    <artifactId>easydoc</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <url>https://github.com/easy-4-java/${project.artifactId}</url>
    <version>1.0.x.20260630-SNAPSHOT</version>
    <description>Building doc documents based on xhtml templates using docx4j</description>
    <packaging>pom</packaging>

    <!-- 开源协议采用 Apache 2.0 协议 -->
    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <!-- 源码仓库（SCM）信息 -->
    <scm>
        <connection>scm:git:https://github.com/easy-4-java/easydoc.git</connection>
        <developerConnection>scm:git:https://github.com/easy-4-java/easydoc.git</developerConnection>
        <url>https://github.com/easy-4-java/easydoc</url>
        <tag>${project.artifactId}</tag>
    </scm>

    <!-- 开发者信息 -->
    <developers>
        <developer>
            <name>Loong Wan</name>
            <email>hiwepy@gmail.com</email>
            <url>https://github.com/loong10k</url>
            <roles>
                <role>developer</role>
            </roles>
            <timezone>+8</timezone>
        </developer>
    </developers>



    <!-- 版本属性定义：Java 版本、依赖版本与 Maven 插件版本统一管理 -->
    <properties>
        <!-- ═══ 第一段: 基础属性（自然排序）═══ -->
        <java.version>1.8</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.version>3.9.16</maven.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- ═══ 第二段: 第三方依赖版本（按字母排序）═══ -->
        <!-- Source: https://mvnrepository.com/artifact/org.antlr/antlr4-runtime -->
        <antlr4.version>4.7.1</antlr4.version>
        <assertj.version>3.27.3</assertj.version>
        <beetl.version>3.1.3.RELEASE</beetl.version>
        <commons-beanutils.version>1.11.0</commons-beanutils.version>
        <commons-io.version>2.22.0</commons-io.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <commons-logging.version>1.3.5</commons-logging.version>
        <docx4j-export-fo.version>8.3.15</docx4j-export-fo.version>
        <docx4j-importxhtml.version>8.3.15</docx4j-importxhtml.version>
        <docx4j-jaxb.version>${docx4j.version}</docx4j-jaxb.version>
        <docx4j-openxml.version>${docx4j.version}</docx4j-openxml.version>
        <docx4j-xhtmlrenderer.version>3.0.0</docx4j-xhtmlrenderer.version>
        <docx4j.version>8.3.15</docx4j.version>
        <freemarker.version>2.3.29</freemarker.version>
        <httl.version>1.0.12</httl.version>
        <jackson.version>2.17.2</jackson.version>
        <javax-activation.version>1.1.1</javax-activation.version>
        <javax.mail-api.version>1.6.2</javax.mail-api.version>
        <jaxb-impl.version>2.3.9</jaxb-impl.version>
        <jetbrick.version>2.1.10</jetbrick.version>
        <jsoup.version>1.13.1</jsoup.version>
        <junit-jupiter.version>5.11.4</junit-jupiter.version>
        <junit.version>4.13.2</junit.version>
        <ognl.version>3.2.13</ognl.version>
        <persistence.version>2.7.6</persistence.version>
        <rythm.version>1.3.0</rythm.version>
        <servlet-api.version>3.0.1</servlet-api.version>
        <slf4j.version>2.0.18</slf4j.version>
        <thymeleaf.version>3.0.11.RELEASE</thymeleaf.version>
        <velocity-tools.version>2.0</velocity-tools.version>
        <velocity.version>2.2</velocity.version>
        <webit-script.version>1.5.2</webit-script.version>
        <!-- Maven Plugin versions -->
        <maven-antrun-plugin.version>1.8</maven-antrun-plugin.version>
        <maven-assembly-plugin.version>3.1.1</maven-assembly-plugin.version>
        <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-dependency-plugin.version>3.8.1</maven-dependency-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
        <maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <maven-install-plugin.version>3.1.4</maven-install-plugin.version>
        <maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-nexus-staging-plugin.version>1.7.0</maven-nexus-staging-plugin.version>
        <maven-nexus-yuicompressor-plugin.version>1.1.0</maven-nexus-yuicompressor-plugin.version>
        <maven-release-plugin.version>3.3.1</maven-release-plugin.version>
        <maven-resources-plugin.version>3.5.0</maven-resources-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-surefire-plugin.version>2.22.1</maven-surefire-plugin.version>
        <maven-war-plugin.version>3.2.2</maven-war-plugin.version>
        <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
        <jaxb-namespaceprefixmapper.version>6.1.0</jaxb-namespaceprefixmapper.version>
        <lombok.version>1.18.46</lombok.version>
    </properties>

    <!-- 制品发布仓库配置（distributionManagement） -->
    <distributionManagement>
        <repository>
            <id>2624322-release-6F6h6R</id>
            <url>https://packages.aliyun.com/6927b116e6c3e0425dbdf60d/maven/2624322-release-6f6h6r</url>
        </repository>
        <snapshotRepository>
            <id>2624322-snapshot-3EoOv3</id>
            <url>https://packages.aliyun.com/6927b116e6c3e0425dbdf60d/maven/2624322-snapshot-3eoov3</url>
        </snapshotRepository>
    </distributionManagement>

    <!-- 项目子模块（modules） -->
    <modules>
        <module>easydoc-beetl</module>
        <module>easydoc-bom</module>
        <module>easydoc-core</module>
        <module>easydoc-freemarker</module>
        <module>easydoc-httl</module>
        <module>easydoc-jetbrick</module>
        <module>easydoc-jsp</module>
        <module>easydoc-rythm</module>
        <module>easydoc-thymeleaf</module>
        <module>easydoc-velocity</module>
        <module>easydoc-webit</module>
        <module>easydoc-xhtml</module>
    </modules>

    <!-- 构建配置（Build） -->
    <build>
        <pluginManagement>
            <plugins>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-clean-plugin -->
                <plugin>
                    <!-- 清理插件：清理构建输出目录 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven-clean-plugin.version}</version>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
                <plugin>
                    <!-- 编译插件：编译主代码至主输出目录 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <!-- 字符集编码 -->
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <!-- 编译最大内存 -->
                        <maxmem>512M</maxmem>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-deploy-plugin -->
                <plugin>
                    <!-- 发布插件：将项目输出构件部署到远程仓库 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-enforcer-plugin -->
                <plugin>
                    <!-- 环境检查插件：代码编译前的环境监察 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>default-cli</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <phase>validate</phase>
                            <configuration>
                                <rules>
                                    <requireMavenVersion>
                                        <message>
	                                        You are running an older version of Maven. This application requires at least Maven ${maven.version}.
										</message>
                                        <version>[${maven.version}.0,)</version>
                                    </requireMavenVersion>
                                    <requireJavaVersion>
                                        <message>
	                                        You are running an older version of Java. This application requires at least JDK ${java.version}.
										</message>
                                        <version>[${java.version}.0,)</version>
                                    </requireJavaVersion>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-gpg-plugin -->
                <plugin>
                    <!-- 签名插件：对制品进行 PGP 签名 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven-gpg-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>sign-artifacts</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>sign</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-install-plugin -->
                <plugin>
                    <!-- 安装插件：将项目输出构件安装到本地仓库 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven-install-plugin.version}</version>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-resources-plugin -->
                <plugin>
                    <!-- 资源插件：复制主资源文件至主输出目录 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-release-plugin -->
                <plugin>
                    <!-- 发布管理插件：版本发布与标签管理 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>${maven-release-plugin.version}</version>
                    <configuration>
                        <tagNameFormat>v@{project.version}</tagNameFormat>
                        <autoVersionSubmodules>true</autoVersionSubmodules>
                        <useReleaseProfile>false</useReleaseProfile>
                        <releaseProfiles>release</releaseProfiles>
                        <goals>deploy</goals>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-source-plugin -->
                <plugin>
                    <!-- 源码插件：发布时自动将源码同时发布 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <configuration>
                        <attach>true</attach>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-surefire-plugin -->
                <plugin>
                    <!-- 单元测试插件：执行测试用例 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                    <configuration>
                        <skip>false</skip>
                        <skipTests>false</skipTests>
                        <argLine>-Xmx1024m -Dfile.encoding=UTF-8 @{argLine}</argLine>
                        <includes>
                            <include>**/*Test.java</include>
                            <include>**/*Tests.java</include>
                        </includes>
                        <excludes>
                            <exclude>**/TestBean.java</exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-jar-plugin -->
                <plugin>
                    <!-- jar 包生成插件：创建项目 jar 包 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                    <configuration>
                        <skipIfEmpty>true</skipIfEmpty>
                        <archive>
                            <manifest>
                                <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                                <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                            </manifest>
                        </archive>
                    </configuration>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-javadoc-plugin -->
                <plugin>
                    <!-- 文档插件：发布时自动生成 Javadoc 并发布 -->
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <configuration>
                        <!-- 原生 doclint=none：additionalparam 在 javadoc-plugin 3.x 已失效，
                             该配置让 release profile 的 javadoc jar 在存量中文注释上不报错 -->
                        <doclint>none</doclint>
                        <charset>${project.build.sourceEncoding}</charset>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <docencoding>${project.build.sourceEncoding}</docencoding>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <phase>package</phase>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- https://mvnrepository.com/artifact/org.sonatype.plugins/nexus-staging-maven-plugin -->
                <plugin>
                    <groupId>org.sonatype.plugins</groupId>
                    <artifactId>nexus-staging-maven-plugin</artifactId>
                    <version>${maven-nexus-staging-plugin.version}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <serverId>ossrh</serverId>
                        <nexusUrl>https://oss.sonatype.org/</nexusUrl>
                        <autoReleaseAfterClose>false</autoReleaseAfterClose>
                        <stagingProgressPauseDurationSeconds>60</stagingProgressPauseDurationSeconds>
                        <stagingProgressTimeoutMinutes>20</stagingProgressTimeoutMinutes>
                        <detectBuildFailures>true</detectBuildFailures>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>

            <!-- https://mvnrepository.com/artifact/org.jacoco/jacoco-maven-plugin -->
            <plugin>
                <!-- 覆盖率插件：生成覆盖率报告并校验测试覆盖率（目标 90%） -->
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <haltOnFailure>false</haltOnFailure>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.90</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
            <plugin>
                <!-- 编译插件：编译主代码至主输出目录 -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <!-- 保留方法参数名，支持 Spring MVC 按参数名绑定 -->
                    <parameters>true</parameters>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <maxmem>512M</maxmem>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- 构建 Profile 配置 -->
    <profiles>
        <!-- JDK 8+ 自动激活：禁用 Javadoc doclint 检查，覆盖 Lombok/JaCoCo/JAXB 版本 -->
        <profile>
            <id>disable-javadoc-doclint</id>
            <activation>
                <jdk>[1.8,)</jdk>
            </activation>
            <properties>
                <central-publishing-maven-plugin.version>0.11.0</central-publishing-maven-plugin.version>
                <lombok.version>1.18.46</lombok.version>
                <jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>
            
		<jaxb-namespaceprefixmapper.version>6.1.0</jaxb-namespaceprefixmapper.version></properties>
        </profile>
        <!-- 发布到 Maven Central Portal：mvn -Prelease deploy（GPG 签名 + 源码 + Javadoc + Central 发布插件） -->

        <profile>
            <id>release</id>
            <build>
                <plugins>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-enforcer-plugin -->
                    <plugin>
                        <!-- 环境检查插件：代码编译前的环境监察 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-enforcer-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-compiler-plugin -->
                    <plugin>
                        <!-- 编译插件：编译主代码至主输出目录 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-resources-plugin -->
                    <plugin>
                        <!-- 资源插件：复制主资源文件至主输出目录 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-surefire-plugin -->
                    <plugin>
                        <!-- 单元测试插件：执行测试用例 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-jar-plugin -->
                    <plugin>
                        <!-- jar 包生成插件：创建项目 jar 包 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-source-plugin -->
                    <plugin>
                        <!-- 源码插件：发布时自动将源码同时发布 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-javadoc-plugin -->
                    <plugin>
                        <!-- 文档插件：发布时自动生成 Javadoc 并发布 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-install-plugin -->
                    <plugin>
                        <!-- 安装插件：将项目输出构件安装到本地仓库 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-install-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-gpg-plugin -->
                    <plugin>
                        <!-- 签名插件：对制品进行 PGP 签名 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-deploy-plugin -->
                    <plugin>
                        <!-- 发布插件：将项目输出构件部署到远程仓库 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-release-plugin -->
                    <plugin>
                        <!-- 发布管理插件：版本发布与标签管理 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-release-plugin</artifactId>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.sonatype.plugins/nexus-staging-maven-plugin -->
                    <plugin>
                        <groupId>org.sonatype.plugins</groupId>
                        <artifactId>nexus-staging-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- 发布到 Maven Central Portal：mvn -Pcentral deploy（GPG 签名 + 源码 + Javadoc + Central 发布插件） -->

        <profile>
            <id>central</id>
            <build>
                <plugins>

                    <!-- https://mvnrepository.com/artifact/org.apache.maven.plugins/maven-gpg-plugin -->
                    <plugin>
                        <!-- 签名插件：对制品进行 PGP 签名 -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven-gpg-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- https://mvnrepository.com/artifact/org.sonatype.central/central-publishing-maven-plugin -->
                    <plugin>
                        <!-- 发布插件：将制品发布到 Maven Central Portal -->
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central-publishing-maven-plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <!-- 依赖版本统一管理（dependencyManagement） -->
    <dependencyManagement>
        <dependencies>
            <!-- ======== Test ======== -->
            <!-- Source: https://mvnrepository.com/artifact/junit/junit -->
            <!-- For Junit -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter -->
            <!-- For JUnit 5 -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit-jupiter.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter-api -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit-jupiter.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.junit.jupiter/junit-jupiter-params -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <version>${junit-jupiter.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.assertj/assertj-core -->
            <!-- For AssertJ -->
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
            </dependency>
            <!-- ======== Servlet / JAX-RS ======== -->
            <!-- Source: https://mvnrepository.com/artifact/javax.servlet/javax.servlet-api -->
            <!-- For Servlet Api -->
            <dependency>
                <groupId>javax.servlet</groupId>
                <artifactId>javax.servlet-api</artifactId>
                <version>${servlet-api.version}</version>
            </dependency>
            <!-- ======== Logging ======== -->
            <!-- Source: https://mvnrepository.com/artifact/org.slf4j/slf4j-api -->
            <!-- For Slf4j -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.slf4j/slf4j-simple -->
            <!-- For Slf4j -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <!-- ======== Commons ======== -->
            <!-- https://mvnrepository.com/artifact/commons-beanutils/commons-beanutils -->
            <!-- For Commons BeanUtils：JavaBean 属性拷贝与操作 -->
            <dependency>
                <groupId>commons-beanutils</groupId>
                <artifactId>commons-beanutils</artifactId>
                <version>${commons-beanutils.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.apache.commons/commons-lang3 -->
            <!-- For Commons Lang3：字符串、对象等通用工具类 -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/commons-io/commons-io -->
            <!-- For Commons IO：文件与流操作工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <!-- ======== HTML/CSS 解析 ======== -->
            <!-- Source: https://mvnrepository.com/artifact/org.jsoup/jsoup -->
            <!-- For Jsoup：HTML 解析与操作库 -->
            <dependency>
                <groupId>org.jsoup</groupId>
                <artifactId>jsoup</artifactId>
                <version>${jsoup.version}</version>
            </dependency>
            <!-- ======== Docx4j（Word 文档处理核心框架）======== -->
            <!-- Source: https://mvnrepository.com/artifact/org.docx4j/docx4j-core -->
            <!-- Docx4j 核心库：OPC 包解析、XML 绑定、文档对象模型 -->
            <!-- 显式排除 jcl-over-slf4j/slf4j-log4j12/log4j，避免与 SLF4J 2.x 桥接冲突 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-core</artifactId>
                <version>${docx4j.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>org.slf4j</groupId>
                        <artifactId>jcl-over-slf4j</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>org.slf4j</groupId>
                        <artifactId>slf4j-log4j12</artifactId>
                    </exclusion>
                    <exclusion>
                        <groupId>log4j</groupId>
                        <artifactId>log4j</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-export-fo -->
            <!-- Docx4j FO 导出：将 Word 文档导出为 PDF（基于 Apache FOP） -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-export-fo</artifactId>
                <version>${docx4j-export-fo.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-docx-anon -->
            <!-- Docx4j 文档匿名化：移除文档中的作者等隐私信息 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-docx-anon</artifactId>
                <version>${docx4j.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-diffx -->
            <!-- Docx4j DiffX：XML 文档差异比较 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-diffx</artifactId>
                <version>${docx4j.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-ImportXHTML -->
            <!-- Docx4j XHTML 导入：将 XHTML 内容转换为 Word 文档段落 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-ImportXHTML</artifactId>
                <version>${docx4j-importxhtml.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/xhtmlrenderer -->
            <!-- Docx4j XHTML 渲染器：基于 Flying Saucer 的 XHTML/CSS 渲染引擎 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>xhtmlrenderer</artifactId>
                <version>${docx4j-xhtmlrenderer.version}</version>
            </dependency>
            <!-- ======== JAXB（XML 绑定实现）======== -->
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-JAXB-Internal -->
            <!-- Docx4j JAXB Internal：docx4j 内置 JAXB 实现 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-JAXB-Internal</artifactId>
                <version>${docx4j-jaxb.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-JAXB-MOXy -->
            <!-- Docx4j JAXB MOXy：基于 EclipseLink MOXy 的 JAXB 运行时 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-JAXB-MOXy</artifactId>
                <version>${docx4j-jaxb.version}</version>
            </dependency>
            <!-- ======== EclipseLink（MOXy JAXB 运行时依赖）======== -->
            <!-- https://mvnrepository.com/artifact/org.eclipse.persistence/eclipselink -->
            <!-- EclipseLink ORM 框架（MOXy JAXB 运行时所需） -->
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>eclipselink</artifactId>
                <version>${persistence.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.eclipse.persistence/org.eclipse.persistence.antlr -->
            <!-- EclipseLink ANTLR 运行时（MOXy 表达式解析所需） -->
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>org.eclipse.persistence.antlr</artifactId>
                <version>${persistence.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.eclipse.persistence/org.eclipse.persistence.asm -->
            <!-- EclipseLink ASM 字节码操作（MOXy 运行时所需） -->
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>org.eclipse.persistence.asm</artifactId>
                <version>${persistence.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.eclipse.persistence/org.eclipse.persistence.core -->
            <!-- EclipseLink 核心（MOXy JAXB 运行时所需） -->
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>org.eclipse.persistence.core</artifactId>
                <version>${persistence.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.eclipse.persistence/org.eclipse.persistence.moxy -->
            <!-- EclipseLink MOXy：JAXB 注解驱动的 XML/JSON 绑定实现 -->
            <dependency>
                <groupId>org.eclipse.persistence</groupId>
                <artifactId>org.eclipse.persistence.moxy</artifactId>
                <version>${persistence.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-JAXB-ReferenceImpl -->
            <!-- Docx4j JAXB ReferenceImpl：基于 GlassFish JAXB RI 的 JAXB 运行时 -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-JAXB-ReferenceImpl</artifactId>
                <version>${docx4j-jaxb.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/com.googlecode.jaxb-namespaceprefixmapper-interfaces/JAXBNamespacePrefixMapper -->
            <!-- JAXB 命名空间前缀映射器：自定义 XML 输出中的命名空间前缀 -->
            <dependency>
                <groupId>com.googlecode.jaxb-namespaceprefixmapper-interfaces</groupId>
                <artifactId>JAXBNamespacePrefixMapper</artifactId>
                <version>${jaxb-namespaceprefixmapper.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/javax.xml.bind/jaxb-api -->
            <!-- 显式锁定 javax JAXB API 2.3.1，JDK 9+ 已移除此包 -->
            <dependency>
                <groupId>javax.xml.bind</groupId>
                <artifactId>jaxb-api</artifactId>
                <version>2.3.1</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/com.sun.xml.bind/jaxb-impl -->
            <!-- 显式锁定 JAXB 实现版本，覆盖 docx4j 传递的旧版本 -->
            <dependency>
                <groupId>com.sun.xml.bind</groupId>
                <artifactId>jaxb-impl</artifactId>
                <version>${jaxb-impl.version}</version>
            </dependency>
            <!-- ======== Docx4j OpenXML Objects（OOXML 模型对象）======== -->
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-openxml-objects -->
            <!-- OpenXML 文档对象（wml：WordprocessingML） -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-openxml-objects</artifactId>
                <version>${docx4j-openxml.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-openxml-objects-pml -->
            <!-- OpenXML 演示文稿对象（pml：PresentationML） -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-openxml-objects-pml</artifactId>
                <version>${docx4j-openxml.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.docx4j/docx4j-openxml-objects-sml -->
            <!-- OpenXML 电子表格对象（sml：SpreadsheetML） -->
            <dependency>
                <groupId>org.docx4j</groupId>
                <artifactId>docx4j-openxml-objects-sml</artifactId>
                <version>${docx4j-openxml.version}</version>
            </dependency>
            <!-- ======== 模板引擎（Template Engines）======== -->
            <!-- Source: https://mvnrepository.com/artifact/ognl/ognl -->
            <!-- OGNL 表达式语言：用于模板中的动态属性访问 -->
            <dependency>
                <groupId>ognl</groupId>
                <artifactId>ognl</artifactId>
                <version>${ognl.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/com.ibeetl/beetl -->
            <!-- Beetl 模板引擎：高性能 Java 模板引擎 -->
            <dependency>
                <groupId>com.ibeetl</groupId>
                <artifactId>beetl</artifactId>
                <version>${beetl.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.freemarker/freemarker -->
            <!-- FreeMarker 模板引擎：通用模板引擎，广泛用于 Web 视图渲染 -->
            <dependency>
                <groupId>org.freemarker</groupId>
                <artifactId>freemarker</artifactId>
                <version>${freemarker.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.perfcake/httl -->
            <!-- HTTL 模板引擎：高性能模板引擎，语法类似 Velocity -->
            <dependency>
                <groupId>org.perfcake</groupId>
                <artifactId>httl</artifactId>
                <version>${httl.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/com.github.subchen/jetbrick-template -->
            <!-- Jetbrick 模板引擎：类 Velocity 语法，支持强类型和宏 -->
            <dependency>
                <groupId>com.github.subchen</groupId>
                <artifactId>jetbrick-template</artifactId>
                <version>${jetbrick.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.antlr/antlr4-runtime -->
            <!-- ANTLR4 运行时：Rythm 等模板引擎的解析器依赖 -->
            <dependency>
                <groupId>org.antlr</groupId>
                <artifactId>antlr4-runtime</artifactId>
                <version>${antlr4.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.rythmengine/rythm-engine -->
            <!-- Rythm 模板引擎：类 C# Razor 语法的 Java 模板引擎 -->
            <dependency>
                <groupId>org.rythmengine</groupId>
                <artifactId>rythm-engine</artifactId>
                <version>${rythm.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.thymeleaf/thymeleaf -->
            <!-- Thymeleaf 模板引擎：自然模板引擎，支持 HTML 原型预览 -->
            <dependency>
                <groupId>org.thymeleaf</groupId>
                <artifactId>thymeleaf</artifactId>
                <version>${thymeleaf.version}</version>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/org.apache.velocity/velocity-engine-core -->
            <!-- Apache Velocity 引擎核心：经典模板引擎 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.apache.velocity/velocity-engine-scripting -->
            <!-- Velocity JSR-223 脚本引擎适配 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-scripting</artifactId>
                <version>${velocity.version}</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.apache.velocity/velocity-tools -->
            <!-- Velocity 工具集：提供日期、数字、日期等常用工具 -->
            <!-- 显式排除 struts-core/struts-taglib/struts-tiles，避免引入过时的 Struts 1.x -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-tools</artifactId>
                <version>${velocity-tools.version}</version>
                <exclusions>
                    <exclusion>
                        <artifactId>struts-core</artifactId>
                        <groupId>org.apache.struts</groupId>
                    </exclusion>
                    <exclusion>
                        <artifactId>struts-taglib</artifactId>
                        <groupId>org.apache.struts</groupId>
                    </exclusion>
                    <exclusion>
                        <artifactId>struts-tiles</artifactId>
                        <groupId>org.apache.struts</groupId>
                    </exclusion>
                </exclusions>
            </dependency>
            <!-- Source: https://mvnrepository.com/artifact/com.github.zqq90.webit-script/webit-script -->
            <!-- Webit-Script 模板引擎：轻量级模板引擎 -->
            <dependency>
                <groupId>com.github.zqq90.webit-script</groupId>
                <artifactId>webit-script</artifactId>
                <version>${webit-script.version}</version>
            </dependency>
            <!-- ======== 工具（Utilities）======== -->
            <!-- Source: https://mvnrepository.com/artifact/org.projectlombok/lombok -->
            <!-- For Lombok：编译期代码生成，减少样板代码 -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 项目依赖（dependencies）：所有子模块共享的传递依赖 -->
    <dependencies>
        <!-- ======== Logging ======== -->
        <!-- For Slf4j：日志门面 API，所有子模块统一使用 -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <!-- ======== Test ======== -->
        <!-- For Junit：JUnit 4 测试框架 -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- For JUnit 5：JUnit Jupiter 测试框架 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- For AssertJ：流式断言库，增强测试可读性 -->
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ======== 工具（Utilities）======== -->
        <!-- For Lombok：编译期注解处理器，provided scope 仅编译期生效 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
