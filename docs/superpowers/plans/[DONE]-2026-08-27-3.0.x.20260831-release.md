# easydoc 3.0.x.20260730 发布计划（日期戳）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan step-by-step. Steps use checkbox (`- [ ]`) syntax for tracking. Each step ends with a `commit:` line that names the expected commit; the implementer commits only after `verify:` is green.

**Goal:** Cut the **first** date-stamp release of the 3.0.x line at `3.0.x.20260730` (no `-SNAPSHOT`), publish to Maven Central, tag it, then bump to the next dev cycle `3.0.x.<next>-SNAPSHOT`. This calibrates with the 1.0.x / 2.0.x convention `X.x.YYYYMMDD` already used for `1.0.x.20260630` / `1.0.x.20260830` / `2.0.x.20260630` / `2.0.x.20260830` (those tags already exist locally and remotely).

**Important constraints (must not be skipped):**
- Use the **same date-stamp format** as the existing 1.0.x / 2.0.x tags — `3.0.x.20260730` (NOT `3.0.0.RELEASE`). The historical GA doc `RELEASING.md` is intentionally NOT updated in this plan (kept as future reference).
- Maven 4.0.0-rc-6+ is required (`requireMavenVersion` enforcer rule). Use `~/tools/apache-maven-4.0.0-rc-6/bin/mvn` explicitly.
- `mvn -P central clean deploy` is known broken on Maven 4 (bundle polluted with `*-consumer.pom*` files; see `docs/release-central.md`). The **proven path** is the manual bundle-fix + Central Portal REST upload — that procedure MUST be followed if `-P central` fails (do not retry deploy blindly).
- DO NOT touch CHANGELOG.md bullet content. Only convert the `## [3.0.x] — Unreleased` heading to a dated release heading once the build is published.
- 1.0.x / 2.0.x sync branches are NOT in scope for this plan — they already carry their own SNAPSHOT bumps; only publish 3.0.x this round.

## Global Constraints

- Publish targets: **Maven Central Portal** via REST upload (proven); fallback Aliyun private repo NOT used in this plan.
- All commits on a fresh release branch `release/3.0.x.20260730`, FF-merged back into `feature/3.0.x` and `main` at the end. The branch and its merges are local until step 8 (push).
- No commits to any branch that already has the same string in its history — search first.
- GPG signing must succeed locally before any deploy step (`gpg --list-secret-keys AF1B6E00` should show the `hiwepy <hiwepy@gmail.com>` key).

## Step 1 — Pre-flight (must all be true before step 2)

- [ ] **Local tree clean** on `feature/3.0.x` (`git status --porcelain` returns nothing).
- [ ] **Origin is reachable** and `feature/3.0.x` is **0 commits ahead of** `origin/feature/3.0.x` (no un-pushed work).
- [ ] **GPG key present**: `gpg --list-secret-keys --keyid-format short` shows `AF1B6E00`.
- [ ] **Central token readable**: `python3 -c "import re,base64; s=open('$HOME/.m2/settings.xml').read(); m=re.search(r'<server>\\s*<id>central</id>\\s*<username>([^<]+)</username>\\s*<password>([^<]+)</password>', s, re.S); print('token ok' if m else 'NO CENTRAL SERVER')"`.
- [ ] **Maven 4 available**: `~/tools/apache-maven-4.0.0-rc-6/bin/mvn --version` reports Apache Maven 4.x and Java 21.
- [ ] **No existing tag collision**: `git ls-remote origin 'refs/tags/3.0.x.20260730'` is empty.
- `commit:` none (preflight)
- `verify:` echo "OK" if all 5 lines above are `✓`.

## Step 2 — Cut release branch and set version

- [ ] `cd /Users/wandl/workspaces/workspace-github-easy-4-java/easydoc && git checkout feature/3.0.x && git checkout -b release/3.0.x.20260730`.
- [ ] In root pom.xml replace `<revision>3.0.x.20260730-SNAPSHOT</revision>` with `<revision>3.0.x.20260730</revision>` (exactly the SNAPSHOT-stripped version, no `mvn versions:set` plugin dependency).
- [ ] Confirm no other module pom carries `<revision>`: `grep -rn '<revision>' --include=pom.xml | grep -v SNAPSHOT` returns only the root pom line.
- [ ] `git diff` shows exactly one line changed (root pom revision).
- `commit:` `release: 3.0.x.20260730 (remove SNAPSHOT)`
- `verify:` `grep -m1 '<revision>' pom.xml` → `3.0.x.20260730` (no SNAPSHOT suffix).

## Step 3 — Pre-publish verify (no deploy)

- [ ] `~/tools/apache-maven-4.0.0-rc-6/bin/mvn -Denforcer.skip=true clean verify` exits 0; **all 13 modules SUCCESS**, total tests >= 1366, 0 failures, 0 errors, jacoco "All coverage checks have been met" on every module.
- [ ] `git status --porcelain` clean.
- `commit:` none (no change).
- `verify:` grep for "BUILD SUCCESS" + "All coverage checks have been met" appearing at least 13 times in the output.

## Step 4 — Build Central bundle (Maven 4 manual fix path)

This step is REQUIRED because `mvn -P central clean deploy` is known to fail on Maven 4 with `-consumer.pom` bundle pollution. The proven fix (from `docs/release-central.md`):

- [ ] `cd /Users/wandl/workspaces/workspace-github-easy-4-java/easydoc && ~/tools/apache-maven-4.0.0-rc-6/bin/mvn -P central clean deploy -DskipTests 2>&1 | tee /tmp/3.0.0-deploy.log`.
- [ ] Bundle present: `ls -la target/central-publishing/central-bundle.zip`.
- [ ] Drop the failed deployment if Maven 4 upload reported one: query Central Portal via REST (`/api/v1/publisher/deployments`) and DELETE the FAILED one; record the FAILED id in the commit message.
- [ ] Build fixed bundle:
  - [ ] `rm -rf /tmp/cbf && mkdir /tmp/cbf && cd /tmp/cbf && unzip -q <repo>/target/central-publishing/central-bundle.zip`.
  - [ ] `find . -name '*-consumer.pom*' -delete`.
  - [ ] For every remaining `.pom` (recursive), replace `${revision}` with `3.0.x.20260730` then **regenerate .md5 / .sha1 / .sha256 / .sha512 / .asc** for each modified pom. Use `gpg --batch --yes --default-key AF1B6E00 --output "$pom.asc" -b "$pom"` (the `.asc` suffix MUST be explicit — `gpg -b` defaults to `.sig` on this machine).
  - [ ] `zip -q -r /tmp/central-bundle-fixed.zip .` — record file count + size.
- [ ] Verify zero `${revision}` remains: `grep -rl '\${revision}' --include='*.pom' /tmp/cbf | wc -l` → 0.
- `commit:` none (artifacts under /tmp only).
- `verify:` `unzip -l /tmp/central-bundle-fixed.zip | tail -2` reports a sane entry count (~270-300 files, ~2.2 MB).

## Step 5 — Publish to Maven Central Portal (REST, AUTOMATIC)

- [ ] Upload the fixed bundle: `curl -X POST -H "Authorization: Bearer <token>" -F "bundle=@/tmp/central-bundle-fixed.zip;type=application/zip" "https://central.sonatype.com/api/v1/publisher/upload?name=easydoc-3.0.x.20260730&publishingType=AUTOMATIC"` returns a deploymentId (UUID format).
- [ ] Poll status every 60s via `POST /api/v1/publisher/status?id=<deploymentId>`; tolerate PUBLISHING for up to 10 minutes; transitions: VALIDATING → PUBLISHING → PUBLISHED.
- [ ] State reaches **PUBLISHED** with `errors` empty. Record the final deploymentId and the `errors` block (should be empty) in the commit message of the tag (step 6).
- [ ] If FAILED: read the per-purl errors (likely javax/jakarta namespace drift, `${revision}` leakage, or checksum mismatch), DROP the deployment, go back to step 4 and re-fix; do NOT retry deploy blindly.
- `commit:` none yet.
- `verify:` `state: PUBLISHED` printed by curl.

## Step 6 — Tag and bump SNAPSHOT

- [ ] `git tag -a 3.0.x.20260730 -m "release 3.0.x.20260730: structured docx-to-Markdown + Sprint 1 audit fixes" ae1425f...<release-sha>`. (Use the actual release-commit SHA from step 2.)
- [ ] In root pom, replace `<revision>3.0.x.20260730</revision>` with `<revision>3.0.x.20260930-SNAPSHOT</revision>` (jump 90 days — previous cadence was 20260630 → 20260830; keeping 2-month cadence).
- [ ] `git diff` shows exactly the version-string change in root pom.
- [ ] `git add -A && git commit -m "chore: next development cycle 3.0.x.20260930-SNAPSHOT"`.
- [ ] `git log --oneline -3` shows release commit, then SNAPSHOT bump commit, on top of `release/3.0.x.20260730`.
- `commit:` `chore: next development cycle 3.0.x.20260930-SNAPSHOT`
- `verify:` `git log --oneline -3` + `git tag | grep 20260630`.

## Step 7 — Merge release branch back, FF main

- [ ] `git checkout feature/3.0.x && git merge --ff-only release/3.0.x.20260730` (no conflicts expected — disjoint files from step 2).
- [ ] `git checkout main && git merge --ff-only feature/3.0.x`.
- [ ] `git checkout feature/3.0.x` (return).
- [ ] `git log --oneline -3` shows feature/3.0.x at the new HEAD.
- `commit:` none (FF merges produce no commit).
- `verify:` `git ls-remote origin feature/3.0.x` is still **empty** (we haven't pushed yet — that's step 8).

## Step 8 — Push to origin (3 branches in one batch)

- [ ] `git push origin release/3.0.x.20260730` (release branch — keep for archaeology).
- [ ] `git push origin feature/3.0.x 3.0.x.20260730` (push the FF'd feature branch AND the new tag in one invocation — both operations are visible in the output).
- [ ] `git push origin main`.
- [ ] `git ls-remote origin feature/1.0.x feature/2.0.x feature/3.0.x main` — confirm 3.0.x and main have moved forward; 1.0.x / 2.0.x unchanged.
- [ ] `git ls-remote origin 'refs/tags/3.0.x.20260730'` returns exactly one ref.
- `commit:` none.
- `verify:` the remote tag is visible; local `git ls-remote origin feature/3.0.x` returns the same SHA as local `git rev-parse feature/3.0.x`.

## Step 9 — Cosmetic collateral fixes (small, included in this plan so the CHANGELOG heading transition is internally consistent)

- [ ] In `CHANGELOG.md`, replace the heading `## [3.0.x] — Unreleased（feature/3.0.x，面向 JDK 21）` with `## [3.0.x.20260730] — 2026-06-30（feature/3.0.x，面向 JDK 21）`. Add a fresh empty `## [3.0.x] — Unreleased` block above for the next dev cycle. (Do NOT touch any other CHANGELOG content.)
- [ ] In `docs/agent-quickstart-docx.md` line 9, replace `<version>3.0.x.20260730-SNAPSHOT</version>` with `<version>3.0.x.20260730</version>`.
- [ ] In `docs/superpowers/plans/archive/2026-08-26-markdown-docx-conversion.md` and `...-easydoc-docx-to-markdown.md`, rename the H1 title to prepend `[DONE] ` (matches the third archived plan). Do NOT touch Status tables.
- `commit:` `docs: post-release cosmetic fixes (CHANGELOG heading + quickstart version + archive [DONE] prefixes)`
- `verify:` `grep -c '20260730' docs/agent-quickstart-docx.md` → 0; `grep -c '\\[DONE\\]' docs/superpowers/plans/archive/*.md` → 3.

## DoD (definition of done)

ALL of the following must be true to consider this plan complete:

1. `git tag | grep 3.0.x.20260730` returns exactly one row.
2. `git ls-remote origin 'refs/tags/3.0.x.20260730'` returns exactly one ref.
3. `git ls-remote origin main` returns the same SHA as local `git rev-parse main`.
4. Central Portal `/api/v1/publisher/status` for the deploymentId returns `PUBLISHED` with empty errors.
5. `CHANGELOG.md` has a dated `[3.0.x.20260730]` section AND a fresh `[3.0.x] — Unreleased` block.
6. `grep -c '\\[DONE\\]' docs/superpowers/plans/archive/*.md` → 3.
7. `grep -c '20260730' docs/agent-quickstart-docx.md` → 0.
8. `release/3.0.x.20260730` branch still exists both locally and on `origin`.
9. The 1.0.x and 2.0.x branches were NOT touched in this plan (still at the SHAs from the end of the previous sync round: feature/1.0.x = 19cdda2, feature/2.0.x = 471695b — confirm by reading them from `git ls-remote` before declaring done).
10. No `-Pcentral clean deploy` was retried blindly on Maven 4 — all deploys went through the bundle-fix + REST upload path.